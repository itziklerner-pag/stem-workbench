youtube-mutations: baseline docs/evidence/step3-youtube/report.json — 26 assertions, 0 failed

ok    R1  the report is not a report at all — the launch never wrote one
        1 red: the app launches from its real entry point and the youtube probe writes a report
ok    R2  THE PAGE HAS NO PLAYER: youtube.com loaded and there is no <video> in it
        1 red: the source view is on the REAL youtube.com, over https, with the page's own <video> in it
ok    R3  THE SOURCE IS NOT YOUTUBE: the run was pointed at a local fixture and reported success
        1 red: the source view is on the REAL youtube.com, over https, with the page's own <video> in it
ok    R4  THE RUN DOES NOT SAY WHAT IT MEASURED: no duration, so an ad cannot be told from a song
        2 reds: the source view is on the REAL youtube.com, over https, with the page's own <video> in it | ...and the run records what it was measuring — id, duration, ad flag, element volume
ok    R5  THE VIEW IS NOT MUTED — the user hears the raw page under the stems
        1 red: THE VIEW IS MUTED FOR ITS WHOLE LIFE — before the first load, and across every navigation it made
ok    R6  A NAVIGATION STARTED UNMUTED — the 1.90 s leak the spike measured, one navigation at a time
        1 red: THE VIEW IS MUTED FOR ITS WHOLE LIFE — before the first load, and across every navigation it made
ok    R7  THE VIDEO NEVER PLAYED: the click landed on a consent wall and the run measured silence
        1 red: the page's own player is PLAYING, started by a real input event and not by `video.play()`
ok    R7b  THE MEASUREMENT WAS TAKEN OVER A PRE-ROLL AD — six real stems separated out of a car commercial
        1 red: ...and what it measured is the CONTENT, not a pre-roll ad — the ad was waited out or skipped
ok    R7c  THE AD NEVER ENDED and the run went ahead anyway
        1 red: ...and what it measured is the CONTENT, not a pre-roll ad — the ad was waited out or skipped
ok    R8  THE SHIPPED PRELOAD DID NOT REPORT IT: the Host cannot see the player move
        1 red: ...and the SHIPPED preload reported it — the Host learns the player moved from its own transport
ok    R9  THE ARM GESTURE IS UNREACHABLE: a menu item with no accelerator, which armShortcut() then cannot answer with
        1 red: the arm gesture is REACHABLE: the application menu carries `Arm this Source` with an accelerator
ok    R10  SESSION NEVER REACHED THE DECK: the deck stays blank and never learns it is armed
        1 red: ...and clicking it armed the deck: SESSION reached the deck's address
ok    R11  THE DECK NEVER ASKED FOR THE CAPTURE: follow() never reached `start`
        1 red: ...and the DECK asked for the capture itself, over its own envelope
ok    R12  CAPTURE_START CARRIES A `tabId` — the extension's field, on a product with no tabs
        1 red: ...and the HOST originated CAPTURE_START to the engine, carrying a minted token and nothing about a tab
ok    R13  THE ENGINE IS NOT RECORDING: the grant was refused and nothing said so
        1 red: the engine is RECORDING the source view, and the ring is being fed REAL AUDIO — frames AND level
ok    R14  THE RING IS FED SILENCE: frames climb at the same rate whether the tab is playing or muted-at-source
        1 red: the engine is RECORDING the source view, and the ring is being fed REAL AUDIO — frames AND level
ok    R15  THE WEIGHTS ARRIVED SHORT: a truncated model file, and the unit's verify would have caught it
        1 red: THE REAL WEIGHTS WENT THROUGH THIS HOST AND A SESSION WAS BUILT FROM THEM
ok    R15b  NO ORT SESSION: the bytes crossed the seam and nothing was built from them
        1 red: THE REAL WEIGHTS WENT THROUGH THIS HOST AND A SESSION WAS BUILT FROM THEM
ok    R16  THE LIVE PIPELINE NEVER RAN: the deck primed and stalled
        1 red: THE LIVE PIPELINE RAN, and the run records whether it KEPT UP on this machine — the p95, the deadline and the drops
ok    R16b  THE INSTRUMENT DID NOT LOOK: no scheduler numbers, so "it kept up" is an opinion
        1 red: THE LIVE PIPELINE RAN, and the run records whether it KEPT UP on this machine — the p95, the deadline and the drops
ok    R17  ONE STEM IS MISSING FROM THE METERS: five stems and a master, reported as six
        1 red: SIX STEMS COME BACK, BY NAME, IN THE UNIT'S OWN ORDER — drums, bass, other, vocals, guitar, piano
ok    R18  THE DECK PAINTS ITS RACK IN THE WRONG ORDER — guitar and piano have no number key, so position IS part of their identity
        1 red: ...and the DECK painted the six as its own rack — the SET is the model's six, the ORDER is `ui/embed.js`'s, and every label matches its own `data-stem`
ok    R18b  A STRIP IS MISLABELLED: the word over the fader is not the stem the fader moves — six correct stems, wrongly named
        1 red: ...and the DECK painted the six as its own rack — the SET is the model's six, the ORDER is `ui/embed.js`'s, and every label matches its own `data-stem`
ok    R18c  THE RACK IS NOT THE MODEL'S SIX: a strip for a stem the separator does not produce
        1 red: ...and the DECK painted the six as its own rack — the SET is the model's six, the ORDER is `ui/embed.js`'s, and every label matches its own `data-stem`
ok    R19  THE DECK PLAYED NOTHING AUDIBLE: the master sits under the presence floor and nothing else notices
        1 red: ...and what the deck PLAYED is audible — master rms above the 0.01 floor, presence not level
ok    R20  THE SEPARATOR THREW instead of separating — the one thing `Backend.separate` is allowed to do besides work
        6 reds: THE SEPARATOR RAN, in this app, over the audio it captured from the page — the Host's own backend and the real weights | ...and the audio it separated came off the muted view at 44 100 Hz, STEREO, with AGC / echo / noise suppression all OFF | SIX STEMS CAME BACK — six plane pairs at the frozen layout, each with its own level | ...and the LABELS ARE THE SEPARATOR'S, not this probe's `STEMS.map`: the `bass` plane has strictly the lowest centroid and strictly the most energy under 500 Hz and under 120 Hz, and it holds 2x the mix's low end while the `vocals` plane holds under a 20th of it | ...and the six SUM BACK to the mix — residual under 0.5x the input, the sum within 1.5x of it, which six copies of one mix could not be | ...and no two of them are the SAME SIGNAL — all 15 pairwise correlations under 0.8, where six differently-scaled copies of one mix would every one be 1.000
ok    R20b  THE SEGMENT NEVER FILLED: the separator ran over a partly-empty buffer
        1 red: THE SEPARATOR RAN, in this app, over the audio it captured from the page — the Host's own backend and the real weights
ok    R20c  IT WAS NOT THE REAL WEIGHTS: a different file went into the backend
        1 red: THE SEPARATOR RAN, in this app, over the audio it captured from the page — the Host's own backend and the real weights
ok    R21  THE CAPTURE IS MONO: half the stereo image gone, and every level still looks plausible
        1 red: ...and the audio it separated came off the muted view at 44 100 Hz, STEREO, with AGC / echo / noise suppression all OFF
ok    R21b  AGC IS ON — the spike's Limitation 6: the level decays 17x over 8 s and the gate cannot see it
        1 red: ...and the audio it separated came off the muted view at 44 100 Hz, STEREO, with AGC / echo / noise suppression all OFF
ok    R21c  THE CAPTURE IS AT 48 kHz: the resampling the whole no-resample design exists to avoid
        1 red: ...and the audio it separated came off the muted view at 44 100 Hz, STEREO, with AGC / echo / noise suppression all OFF
ok    R21d  THE SEGMENT IS SILENT: a capture that opened on a muted element and separated nothing
        1 red: ...and the audio it separated came off the muted view at 44 100 Hz, STEREO, with AGC / echo / noise suppression all OFF
ok    R22  FIVE STEMS CAME BACK, NOT SIX
        3 reds: SIX STEMS CAME BACK — six plane pairs at the frozen layout, each with its own level | ...and the LABELS ARE THE SEPARATOR'S, not this probe's `STEMS.map`: the `bass` plane has strictly the lowest centroid and strictly the most energy under 500 Hz and under 120 Hz, and it holds 2x the mix's low end while the `vocals` plane holds under a 20th of it | ...and no two of them are the SAME SIGNAL — all 15 pairwise correlations under 0.8, where six differently-scaled copies of one mix would every one be 1.000
ok    R22b  THE PLANES ARE IN THE WRONG ORDER: six correct stems, mislabelled at the buffer layout
        2 reds: SIX STEMS CAME BACK — six plane pairs at the frozen layout, each with its own level | ...and the LABELS ARE THE SEPARATOR'S, not this probe's `STEMS.map`: the `bass` plane has strictly the lowest centroid and strictly the most energy under 500 Hz and under 120 Hz, and it holds 2x the mix's low end while the `vocals` plane holds under a 20th of it
ok    R23  SIX COPIES OF THE MIX: the fan-out a stalled separator publishes — it sums to six times the input
        1 red: ...and the six SUM BACK to the mix — residual under 0.5x the input, the sum within 1.5x of it, which six copies of one mix could not be
ok    R23b  THE STEMS DO NOT ADD BACK: a residual as large as the input, which is not a masking separator at all
        1 red: ...and the six SUM BACK to the mix — residual under 0.5x the input, the sum within 1.5x of it, which six copies of one mix could not be
ok    R24  SIX IDENTICAL STEMS: the same signal six times, which every level check but this one calls a pass
        1 red: ...and no two of them are the SAME SIGNAL — all 15 pairwise correlations under 0.8, where six differently-scaled copies of one mix would every one be 1.000
ok    R24b  ONE STEM IS DIGITAL SILENCE — a plane the separator never wrote
        1 red: ...and no two of them are the SAME SIGNAL — all 15 pairwise correlations under 0.8, where six differently-scaled copies of one mix would every one be 1.000
ok    R24c  SIX SCALED COPIES OF THE MIX: residual 0, sum ratio 1.0, six distinct levels — and one signal
        2 reds: ...and the LABELS ARE THE SEPARATOR'S, not this probe's `STEMS.map`: the `bass` plane has strictly the lowest centroid and strictly the most energy under 500 Hz and under 120 Hz, and it holds 2x the mix's low end while the `vocals` plane holds under a 20th of it | ...and no two of them are the SAME SIGNAL — all 15 pairwise correlations under 0.8, where six differently-scaled copies of one mix would every one be 1.000
ok    R22c  THE BACKEND WROTE ITS PLANES IN ANOTHER ORDER: the six names stay put and the audio under them rotates
        1 red: ...and the LABELS ARE THE SEPARATOR'S, not this probe's `STEMS.map`: the `bass` plane has strictly the lowest centroid and strictly the most energy under 500 Hz and under 120 Hz, and it holds 2x the mix's low end while the `vocals` plane holds under a 20th of it
ok    R25  THE DECK IS BLANK: a photograph of a view that painted one colour
        1 red: the deck is PAINTED while the stems are live — a photograph of the running app, not a blank view
ok    R26  THE CAPTURE DIED ON RELOAD: the frame counter froze when the document went
        1 red: THE CAPTURE SURVIVES A FULL PAGE RELOAD — the grant is against the view, not the document
ok    R27  THE BUS DROPPED TRAFFIC FOR WANT OF A LISTENER — the quietest failure on the whole seam
        1 red: nothing on the bus was dropped for want of a listener while all that happened

coverage: 26/26 assertions were turned red by some row
(the product rows L1-L3 were NOT run — `--live` runs them, one real launch each)

youtube-mutations: 43 passed, 0 failed
